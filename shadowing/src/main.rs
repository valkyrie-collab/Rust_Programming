fn main() {
    let x: i32 = 5;
    let x: i32 = x + 1;
    let x: i32 = x * 2;

    println!("The value of x {}", x);

    let spaces: &str = "  ";
    let spaces: usize = spaces.len();

    println!("The size of spaces: {}", spaces);

    // let mut sp: &str = "   ";
    // sp = sp.len();
}
